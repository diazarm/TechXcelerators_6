/**
 * Tipos co-locados para el componente Galeria
 * 
 * Define las interfaces específicas del componente Galeria
 * siguiendo las reglas del proyecto para tipos co-locados.
 */

export interface GalleryImage {
  id: string;
  src: string;
  alt: string;
  title: string;
  description: string;
  website: string;
}
